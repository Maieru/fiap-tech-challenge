apiVersion: v1
kind: Service
metadata:
  name: fiap-frontend-service
  namespace: fiap-frontend
spec:
  type: ClusterIP
  selector:
    app: fiap-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
